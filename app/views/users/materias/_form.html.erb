<div class="row">
  <div class="col-md-6">
    <div class="card shadow-lg">
      <div class="card-body">
        <div class="box">
          <%= simple_form_for [@user, @materia] do |f| %>
            <%= f.input :codigo %>
            <%= f.input :nombre %>
            <%= f.button :submit, class:"btn btn-primary" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
