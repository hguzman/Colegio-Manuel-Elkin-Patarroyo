<div class="row">
  <div class="col-md-6 m-auto">
    <%= simple_form_for [@usuario, @materia] do |f| %>
      <%= f.input :codigo %>
      <%= f.input :nombre %>
      <%= f.button :submit, class:"btn btn-primary" %>
    <% end %>
  </div>
</div>